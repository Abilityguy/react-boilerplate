import { Button } from "@/components/ui/button";

export default function RegisterAndDownloadButton() {
  return (
    <Button className="bg-teal-400 hover:bg-teal-500 text-white font-semibold rounded-none shadow-lg -mt-1">
      Register & Download
    </Button>
  );
}